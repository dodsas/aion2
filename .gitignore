# Python 캐시
__pycache__/
*.py[cod]

# IDE
.idea/
*.iml

# 런타임 · 락 · 로그
.server.pid
.update.lock
logs/

# 시크릿 (Jenkins 접속정보 등 — 절대 커밋 금지)
.env
.env.*


# SQLite 임시 파일 (WAL 모드)
*.db-shm
*.db-wal

# 캐릭터 상세 캐시 DB (서버가 자동 생성 · 재생성 가능)
char_cache.db

# 크롤 원본 스냅샷 (crawl_craft.py 로 재생성 가능 · 디버그용)
raw/
